---
paths:
  - "src/router.ts"
  - ".myorch/memory/routing.jsonl"
---
# Routing Rule

Use `ccusage --json` before work. Planning/evaluation prefer Claude. Implementation/metareview prefer Codex. At 80% usage, route to the other model unless a manual override is active.
